server:
  port: 8010

spring:
  application:
    name: "discovery-server"

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF